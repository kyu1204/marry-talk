import { ChatInterface } from "@/components/chat-interface-main"

export default function HomePage() {
  return (
    <main className="h-screen overflow-hidden bg-background">
      <ChatInterface />
    </main>
  )
}
